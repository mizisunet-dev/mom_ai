<!--
Skeleton for a portfolio-quality README. Delete every section that does not
apply — an empty heading is worse than a missing one. Replace every ALL-CAPS
token; the audit script fails the repo if any survive.
-->

# PROJECT NAME

ONE SENTENCE: what it does and who it is for.

[![CI](https://github.com/OWNER/REPO/actions/workflows/ci.yml/badge.svg)](https://github.com/OWNER/REPO/actions/workflows/ci.yml)
[![License: MIT](https://img.shields.io/badge/license-MIT-blue.svg)](LICENSE)

![Screenshot of PROJECT NAME](docs/demo.png)

<!-- No screenshot? Paste a real terminal session instead:
```console
$ command --flag input
✓ actual output, copied from a real run
```
-->

## Why

PARAGRAPH: the problem this solves, and what the existing options don't do.
Two to four sentences. Skip the marketing adjectives.

## Install

Requires PREREQUISITE VERSION.

```bash
git clone https://github.com/OWNER/REPO.git
cd REPO
INSTALL COMMAND
```

<!-- Configuration, only if the project cannot run without it: -->

Copy `.env.example` to `.env` and set:

| Variable | What it is |
| --- | --- |
| `VAR_NAME` | WHAT IT IS AND WHERE TO GET IT |

## Usage

```bash
MOST COMMON COMMAND
```

```console
REAL OUTPUT
```

SECOND MOST COMMON CASE, one or two more blocks. Full options in
`COMMAND --help`.

## How it works

OPTIONAL, two to five sentences on the one design decision that isn't
obvious, and why you chose it over the obvious alternative. Delete if the
project has no such decision.

## Development

```bash
TEST COMMAND
LINT COMMAND
```

## Limitations

- WHAT IT DOES NOT DO, stated plainly.
- KNOWN ROUGH EDGE.

## License

MIT — see [LICENSE](LICENSE).
